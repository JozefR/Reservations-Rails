<%= simple_form_for(@room) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :building %>
    <%= f.input :title %>
    <%= f.input :code %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, cancel: rooms_path%>
  </div>
<% end %>
